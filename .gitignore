# Local Netlify folder
.netlify
dist
node_modules
*.orig
__pycache__
dist.frozen
.vscode
__target__
*.swp
.agent
lib/.claude
.pnpm-store/

# Player backups: personal save data, refetchable with --player-id. Never commit.
wasmegg/ascension-planner/*.json
!wasmegg/ascension-planner/package.json
!wasmegg/ascension-planner/tsconfig.json

# Headless search CLI build output
dist-search/

# Vite machine-local env (proxy URLs, preview hostnames)
.env.local
.env.*.local

# macOS Finder metadata. Appears in any directory the Finder has opened and is
# never wanted in a commit.
.DS_Store
